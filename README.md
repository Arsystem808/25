# AI Trading — Starter Repo

Streamlit+Plotly UI, живые котировки (Yahoo/Polygon), приватная стратегия.
Запуск:
```
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
streamlit run app/main.py
```
